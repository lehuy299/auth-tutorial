import CardWrapper from "./card-wrapper";

interface LoginFormProps {
    label: string;
}

export const LoginForm = ({
}: LoginFormProps) => {
    return (
        <CardWrapper>
            Login Form!
        </CardWrapper>
    )
}
